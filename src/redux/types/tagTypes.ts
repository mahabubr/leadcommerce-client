export enum tagTypes {
  user = "user",
  product = "product",
  category = "category",
  store = "store",
  orders = "orders",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.category,
  tagTypes.store,
  tagTypes.orders,
];
