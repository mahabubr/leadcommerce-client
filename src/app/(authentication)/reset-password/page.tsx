import PageBody from "./PageBody";

export const metadata = {
    title: 'LC - reset page',
    description: 'page about page description',
}

const page = () => {
    return (
        <PageBody />
    );
};

export default page;